PRECIOUS FIX PACK v27 — REPLACEMENT FILES
==========================================
Copy these files over your repo (same paths), commit, push, redeploy.

NEW FILES:
  precious-fixes-v27.cjs        → repo root
  PATCH-v27.cjs                 → repo root

REPLACED FILES (already patched):
  mias/index.js                 → v27 bootstrap appended at END + picker internals exposed
  precious-all-packs-boot.cjs   → v27 registered as the FINAL pack
  server.js                     → manifest load report + PATCH-v27 added to boot chain
  index.js                      → PATCH-v27 added to boot chain

WHAT'S FIXED:
  1. tkick/tempkick/tk — re-add persists to nexstore/tkick/tkick_pending.json
     (volume — survives redeploys). Entry written BEFORE the kick; replayed on
     every reconnect; 4 re-add attempts; invite-link DM fallback.
  2. pin/unpin — real pinInChat envelope tried FIRST (old code sent a payload
     WhatsApp silently ignored → "pinned!" but nothing pinned).
  3. tt picker — native button taps (BTN:1.3, list rows) now route into the
     picker through the same normalizer as typed replies.
  4. movie/nkiri — v24's link-only handlers removed; documents delivered again.
  5. video/yt/ytdl — restored to the OLD pre-v24 implementation (magic-byte
     validation, doc/hd/videonote/audio formats, inline→document fallback).
  6. Boot log now prints: "[manifest] N files loaded, M failed" and lists any
     file that didn't load. Pack installs print [packs] lines; v27 prints
     "[precious-v27] ✅ installed — {...}".

CHECK YOUR RAILWAY LOGS AFTER DEPLOY FOR:
  [manifest] 31 files loaded, 0 failed
  [packs] ✅ precious-fixes-v27 installed
  [precious-v27] ✅ installed — {"ok":true,...}
